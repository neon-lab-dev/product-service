server:
  port: 8802

spring:
  datasource:
    driver: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/kaseradb
    username: root
    password: Snd@1142000
  jpa:
    hibernate:
      ddl-auto: update
    database: MYSQL

#file related configuration
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB


